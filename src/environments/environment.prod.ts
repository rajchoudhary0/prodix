const BaseUrl=`http://demo.fullstack.com:9075/v1/`;
export const environment = {
  production: true,
  sendFeedback:`${BaseUrl}sendFeedBack`,
  randomRecord:`${BaseUrl}random`,
  nextWord:`${BaseUrl}nextWord`,
  prvcWord:`${BaseUrl}backWord`,
  searchWord:`${BaseUrl}searchWord`,
  audioPath:`${BaseUrl}`,
  selectAll:`${BaseUrl}/selectAll`


};
